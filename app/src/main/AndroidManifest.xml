<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.cxct.sportlottery">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".MultiLanguagesApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="${app_name}"
        android:networkSecurityConfig="${NETWORK_SECURITY_CONFIG}"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!-- 後臺服務-長連線 -->
        <service android:name=".service.BackService" />
        <!--broadCast-->
        <receiver
            android:name=".ui.home.broadcast.ServiceBroadcastReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.INPUT_METHOD_CHANGED" />
            </intent-filter>
        </receiver>

        <activity android:name=".ui.infoCenter.InfoCenterActivity" />
        <activity android:name=".ui.finance.FinanceActivity" />
        <activity android:name=".ui.money.recharge.MoneyRechargeActivity" />
        <activity android:name=".ui.withdraw.WithdrawActivity" />
        <activity android:name=".ui.withdraw.BankActivity" />
        <activity android:name=".ui.results.ResultsSettlementActivity" />
        <activity android:name=".ui.bet.record.BetRecordActivity" />
        <activity android:name=".ui.helpCenter.HelpCenterActivity" />
        <activity android:name=".ui.splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.home.MainActivity" />
        <activity
            android:name=".ui.login.signIn.LoginActivity"
            android:windowSoftInputMode="adjustResize|stateHidden" />
        <activity
            android:name=".ui.login.signUp.RegisterActivity"
            android:windowSoftInputMode="adjustResize|stateHidden" />
        <activity android:name=".ui.profileCenter.ProfileCenterActivity" />
        <activity android:name=".ui.profileCenter.profile.ProfileActivity" />
        <activity android:name=".ui.profileCenter.nickname.ChangeNicknameActivity" />
        <activity android:name=".ui.profileCenter.changePassword.SettingPasswordActivity" />
        <activity android:name=".ui.profileCenter.versionUpdate.VersionUpdateActivity" />
        <activity android:name=".ui.common.WebActivity" />


        <!-- Since JCore2.0.0 Required SDK核心功能-->
        <!-- 可配置android:process参数将Service放在其他进程中；android:enabled属性不能是false -->
        <!-- 这个是自定义Service，要继承极光JCommonService，可以在更多手机平台上使得推送通道保持的更稳定 -->
        <service
            android:name=".service.PushService"
            android:enabled="true"
            android:exported="false"
            android:process=":pushcore">
            <intent-filter>
                <action android:name="cn.jiguang.user.service.action" />
            </intent-filter>
        </service>

        <!-- Required since 3.0.7 -->
        <!-- 新的 tag/alias 接口结果返回需要开发者配置一个自定的广播 -->
        <!-- 3.3.0开始所有事件将通过该类回调 -->
        <!-- 该广播需要继承 JPush 提供的 JPushMessageReceiver 类, 并如下新增一个 Intent-Filter -->
        <receiver
            android:name=".receiver.PushMessageReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="cn.jpush.android.intent.RECEIVE_MESSAGE" />
                <category android:name="${JPUSH_PKGNAME}" />
            </intent-filter>
        </receiver>

        <!-- SDK 24 以上 要使用 FileProvider 獲取 File 的 Uri，才能提供 file 給外部 app 使用 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>
    </application>

</manifest>